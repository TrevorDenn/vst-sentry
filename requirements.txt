# VST-Sentry — Python Dependencies
# Install:  pip install -r requirements.txt

# Core analysis — PE (Portable Executable) file parser
pefile>=2023.2.7

# VirusTotal API integration — HTTP client
requests>=2.28.0

# ---------------------------------------------------------------------------
# Optional (already included in the Python standard library)
# ---------------------------------------------------------------------------
# tkinter        — GUI framework  (ships with most Python installs)
# configparser   — INI config     (stdlib)
# hashlib        — SHA-256        (stdlib)
# math           — Shannon entropy (stdlib)

# ---------------------------------------------------------------------------
# Optional — Enhanced drag-and-drop support
# ---------------------------------------------------------------------------
# tkinterdnd2>=0.3.0   # Uncomment for native OS drag-and-drop on the drop zone.
#                       # The GUI degrades gracefully to a click-to-browse fallback
#                       # if this package is not installed.

# ---------------------------------------------------------------------------
# Development / Testing
# ---------------------------------------------------------------------------
# pytest>=7.0         # Uncomment to run the test suite
